<Page
    x:Class="SchoolDatabase.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SchoolDatabase"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Grid HorizontalAlignment="Stretch" Margin="0,0,0,0" VerticalAlignment="Stretch">

        <Grid.FocusVisualPrimaryBrush>
            <ImageBrush ImageSource="/Assets/CAMPUS.png"/>
        </Grid.FocusVisualPrimaryBrush>

        <Button x:Name="LoginButton" Content="Login" HorizontalAlignment="Left" Height="62" Margin="455,739,0,0" VerticalAlignment="Top" Width="587" FontSize="36" Click="LoginButton_Click" FontWeight="Normal"/>
        <Image x:Name="School_Logo" HorizontalAlignment="Center" Height="267" Margin="0,104,0,0" VerticalAlignment="Top" Width="338" Source="Assets/SDSULOGO.png"/>
        <TextBox x:Name="LastNameTextBox" Text="" RequestedTheme="Light" Margin="0,531,0,416" FontFamily="Segoe UI" FontSize="24" Opacity="0.9" BorderThickness="4,4,4,4" PlaceholderText="Last Name" Visibility="Visible" Height="53" Width="945" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <TextBox x:Name="UserIDTextBox" Text="" RequestedTheme="Light" Margin="0,609,0,338"  FontFamily="Segoe UI" FontSize="24" Opacity="0.9" BorderThickness="4,4,4,4" PlaceholderText="User ID" Visibility="Visible" Height="53" Width="945"/>
        <!--<Border BorderBrush="Black" HorizontalAlignment="Center" Height="1000" VerticalAlignment="Center" Width="1500" BorderThickness="2,2,2,2" Margin="0,0,0,0"/>-->
        <ComboBox x:Name="SelectPositionComboBox" HorizontalAlignment="Center" Margin="0,425,0,0" VerticalAlignment="Top" Height="44" Width="187" PlaceholderText="Select Position..." FontSize="20">
            <ComboBoxItem Content="Admin"/>
            <ComboBoxItem Content="Professor"/>
            <ComboBoxItem Content="Student"/>
        </ComboBox>
        <!--<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel>
                <TextBox Name="Input_Box"></TextBox>
                <Button Click="AddData">Add</Button>
                <ListView Name="Output">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>-->

    </Grid>
</Page>
